---
import Layout from '../layouts/Layout.astro';
import Navbar from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';
---

<Layout title="Sobre el Dr. Villoria - Especialista en Aparato Digestivo">
  <Navbar />
  
  <!-- Hero Section -->
  <section class="about-hero">
    <div class="hero-background">
      <img src="/assets/medweb_background.avif" loading="eager" alt="Background" class="background-image">
      <div class="background-overlay"></div>
    </div>
    
    <div class="container">
      <h1 class="hero-title">Sobre el Dr. Albert Villoria</h1>
      <p class="hero-subtitle">Especialista en Aparato Digestivo con más de 20 años de experiencia</p>
    </div>
  </section>

  <!-- Main Content -->
  <section class="about-content-section">
    <div class="container">
      <div class="profile-grid">
        <div class="profile-image-wrapper">
          <img src="/assets/profile-pic.png" alt="Dr. Albert Villoria" class="profile-image" />
          <div class="experience-badge">
            <span class="years">20+</span>
            <span class="text">Años de experiencia</span>
          </div>
        </div>
        
        <div class="profile-content">
          <h2 class="content-title">Trayectoria Profesional</h2>
          <p class="content-text">
            Como especialista en Aparato Digestivo, me dedico a proporcionar atención médica de excelencia con un enfoque personalizado y humano. Mi compromiso es ofrecer a cada paciente el mejor cuidado posible, basándome en la evidencia científica más reciente y en una amplia experiencia clínica.
          </p>
          <p class="content-text">
            Durante más de dos décadas, he combinado la práctica clínica con la investigación y la docencia, lo que me permite estar a la vanguardia de los avances en gastroenterología y ofrecer a mis pacientes las opciones terapéuticas más innovadoras y efectivas.
          </p>
        </div>
      </div>

      <!-- Academic Credentials -->
      <div class="credentials-section">
        <h2 class="section-heading">Formación y Credenciales</h2>
        
        <div class="credentials-grid">
          <div class="credential-card">
            <div class="credential-icon">
              <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 10v6M2 10l10-5 10 5-10 5z"/><path d="M6 12v5c3 3 9 3 12 0v-5"/></svg>
            </div>
            <h3 class="credential-title">Formación Académica</h3>
            <p class="credential-description">Doctor en Medicina por la Universidad Autónoma de Barcelona (UAB) con Premio Extraordinario de Doctorado.</p>
          </div>
          
          <div class="credential-card">
            <div class="credential-icon">
              <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20.42 4.58a5.4 5.4 0 0 0-7.65 0l-.77.78-.77-.78a5.4 5.4 0 0 0-7.65 0C1.46 6.7 1.33 10.28 4 13l8 8 8-8c2.67-2.72 2.54-6.3.42-8.42z"></path></svg>
            </div>
            <h3 class="credential-title">Especialización</h3>
            <p class="credential-description">Especialista en Aparato Digestivo desde 2003 y Fellow in Gastroenterology por la European Board of Gastroenterology desde 2006.</p>
          </div>
          
          <div class="credential-card">
            <div class="credential-icon">
              <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M2 3h6a4 4 0 0 1 4 4v14a3 3 0 0 0-3-3H2z"></path><path d="M22 3h-6a4 4 0 0 0-4 4v14a3 3 0 0 1 3-3h7z"></path></svg>
            </div>
            <h3 class="credential-title">Investigación</h3>
            <p class="credential-description">Autor o coautor en más de 50 publicaciones en revistas científicas nacionales e internacionales de reconocido prestigio.</p>
          </div>
          
          <div class="credential-card">
            <div class="credential-icon">
              <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"></path><circle cx="9" cy="7" r="4"></circle><path d="M23 21v-2a4 4 0 0 0-3-3.87"></path><path d="M16 3.13a4 4 0 0 1 0 7.75"></path></svg>
            </div>
            <h3 class="credential-title">Docencia</h3>
            <p class="credential-description">Profesor Asociado en la Facultad de Medicina de la Universidad Autónoma de Barcelona.</p>
          </div>
        </div>
      </div>

      <!-- Professional Memberships -->
      <div class="memberships-section">
        <h2 class="section-heading">Afiliaciones Profesionales</h2>
        <div class="memberships-list">
          <div class="membership-item">
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"></polyline></svg>
            <span>Miembro de la Academia de Ciencias Médicas de Catalunya y Balears</span>
          </div>
          <div class="membership-item">
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"></polyline></svg>
            <span>Miembro de la Sociedad Catalana de Digestología</span>
          </div>
          <div class="membership-item">
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"></polyline></svg>
            <span>Fellow of the European Board of Gastroenterology</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="cta-section">
    <div class="container">
      <div class="cta-content">
        <h2 class="cta-title">¿Necesita una consulta?</h2>
        <p class="cta-description">Estoy aquí para ayudarle con cualquier problema digestivo. Solicite una cita hoy mismo.</p>
        <div class="cta-buttons">
          <a href="/cita" class="btn-primary">
            Solicitar Cita
            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="5" y1="12" x2="19" y2="12"></line><polyline points="12 5 19 12 12 19"></polyline></svg>
          </a>
          <a href="/" class="btn-secondary">Volver al inicio</a>
        </div>
      </div>
    </div>
  </section>

  <Footer />
</Layout>

<style>
  /* Hero Section */
  .about-hero {
    position: relative;
    min-height: 40vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 10rem 0 6rem;
    overflow: hidden;
  }

  .hero-background {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    z-index: 0;
  }

  .background-image {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
  }

  .background-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(135deg, 
      var(--color-primary) 0%, 
      var(--color-secondary) 100%
    );
  }

  .container {
    position: relative;
    z-index: 1;
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  .hero-title {
    font-size: clamp(2.5rem, 5vw, 4rem);
    font-weight: 800;
    color: white;
    text-align: center;
    margin-bottom: 1rem;
  }

  .hero-subtitle {
    font-size: clamp(1.125rem, 2vw, 1.5rem);
    color: rgba(255, 255, 255, 0.9);
    text-align: center;
    font-weight: 500;
  }

  /* Main Content Section */
  .about-content-section {
    padding: 6rem 0;
    background: linear-gradient(to bottom, #ffffff 0%, #f8f9fa 100%);
  }

  .profile-grid {
    display: grid;
    grid-template-columns: 350px 1fr;
    gap: 4rem;
    align-items: start;
    margin-bottom: 5rem;
  }

  .profile-image-wrapper {
    position: relative;
  }

  .profile-image {
    width: 100%;
    height: auto;
    border-radius: var(--radius-2xl);
    box-shadow: var(--shadow-2xl);
  }

  .experience-badge {
    position: absolute;
    bottom: -20px;
    right: -20px;
    background: linear-gradient(135deg, var(--color-primary), var(--color-secondary));
    color: white;
    padding: 1rem;
    border-radius: var(--radius-full);
    width: 110px;
    height: 110px;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    text-align: center;
    box-shadow: var(--shadow-2xl);
    border: 4px solid white;
  }

  .years {
    font-size: 2rem;
    font-weight: 800;
    line-height: 1;
    margin-bottom: 0.25rem;
  }

  .text {
    font-size: 0.75rem;
    line-height: 1.2;
    font-weight: 600;
  }

  .profile-content {
    padding-top: 2rem;
  }

  .content-title {
    font-size: 2rem;
    font-weight: 800;
    color: #1e3a8a;
    margin-bottom: 1.5rem;
  }

  .content-text {
    font-size: 1.125rem;
    line-height: 1.8;
    color: #475569;
    margin-bottom: 1.5rem;
  }

  /* Credentials Section */
  .credentials-section {
    margin-bottom: 5rem;
  }

  .section-heading {
    font-size: 2.5rem;
    font-weight: 800;
    color: #1e3a8a;
    text-align: center;
    margin-bottom: 3rem;
  }

  .credentials-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 2rem;
  }

  .credential-card {
    background: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(10px);
    border-radius: var(--radius-2xl);
    padding: 2.5rem;
    border: 2px solid rgba(14, 165, 233, 0.15);
    box-shadow: var(--shadow-lg);
    transition: all 0.3s ease;
  }

  .credential-card:hover {
    transform: translateY(-8px);
    box-shadow: var(--shadow-2xl);
    border-color: rgba(14, 165, 233, 0.3);
  }

  .credential-icon {
    width: 64px;
    height: 64px;
    background: linear-gradient(135deg, var(--color-primary), var(--color-secondary));
    border-radius: var(--radius-lg);
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 1.5rem;
    color: white;
  }

  .credential-title {
    font-size: 1.25rem;
    font-weight: 700;
    color: #1e3a8a;
    margin-bottom: 1rem;
  }

  .credential-description {
    font-size: 1rem;
    line-height: 1.7;
    color: #475569;
    margin: 0;
  }

  /* Memberships Section */
  .memberships-section {
    background: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(10px);
    border-radius: var(--radius-2xl);
    padding: 3rem;
    border: 2px solid rgba(14, 165, 233, 0.15);
    box-shadow: var(--shadow-lg);
  }

  .memberships-list {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    max-width: 800px;
    margin: 0 auto;
  }

  .membership-item {
    display: flex;
    align-items: center;
    gap: 1rem;
    font-size: 1.125rem;
    color: #475569;
    padding: 1rem;
    background: linear-gradient(135deg, rgba(14, 165, 233, 0.05), rgba(16, 185, 129, 0.05));
    border-radius: var(--radius-lg);
  }

  .membership-item svg {
    flex-shrink: 0;
    color: var(--color-secondary);
  }

  /* CTA Section */
  .cta-section {
    padding: 5rem 0;
    background: linear-gradient(135deg, #1e3a8a 0%, #1e40af 100%);
  }

  .cta-content {
    text-align: center;
    max-width: 800px;
    margin: 0 auto;
  }

  .cta-title {
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 800;
    color: white;
    margin-bottom: 1.5rem;
  }

  .cta-description {
    font-size: 1.25rem;
    color: rgba(255, 255, 255, 0.9);
    margin-bottom: 2.5rem;
    line-height: 1.6;
  }

  .cta-buttons {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  .btn-primary,
  .btn-secondary {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    padding: 1rem 2rem;
    text-decoration: none;
    font-size: 1.125rem;
    font-weight: 700;
    border-radius: var(--radius-full);
    transition: all 0.3s ease;
  }

  .btn-primary {
    background: white;
    color: #1e3a8a;
    box-shadow: var(--shadow-lg);
  }

  .btn-primary:hover {
    background: linear-gradient(135deg, var(--color-primary), var(--color-secondary));
    color: white;
    transform: translateY(-2px);
    box-shadow: var(--shadow-2xl);
  }

  .btn-secondary {
    background: transparent;
    color: white;
    border: 2px solid white;
  }

  .btn-secondary:hover {
    background: white;
    color: #1e3a8a;
    transform: translateY(-2px);
  }

  /* Responsive */
  @media (max-width: 968px) {
    .about-hero {
      padding: 8rem 0 4rem;
    }

    .profile-grid {
      grid-template-columns: 1fr;
      gap: 3rem;
      text-align: center;
    }

    .profile-image-wrapper {
      max-width: 350px;
      margin: 0 auto;
    }

    .experience-badge {
      right: -10px;
      bottom: -10px;
      width: 90px;
      height: 90px;
    }

    .years {
      font-size: 1.75rem;
    }

    .text {
      font-size: 0.65rem;
    }

    .credentials-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 640px) {
    .about-hero {
      padding: 7rem 0 3rem;
    }

    .cta-buttons {
      flex-direction: column;
    }

    .btn-primary,
    .btn-secondary {
      width: 100%;
      justify-content: center;
    }
  }
</style>

<script>
  // Intersection Observer for scroll animations
  document.addEventListener('DOMContentLoaded', function() {
    const observerOptions = {
      root: null,
      rootMargin: '0px',
      threshold: 0.1
    };

    const observer = new IntersectionObserver((entries, observer) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          const target = entry.target as HTMLElement;
          const delay = target.getAttribute('data-delay') || '0';
          
          setTimeout(() => {
            target.classList.add('is-visible');
          }, parseInt(delay));
          
          observer.unobserve(target);
        }
      });
    }, observerOptions);

    document.querySelectorAll('.transition-item').forEach(item => {
      observer.observe(item);
    });
  });
</script>
